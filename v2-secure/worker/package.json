{
  "name": "adjust-roas-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies Adjust Reporting API for the Adjust ROAS Overlay extension. Holds the Adjust API token + per-app tokens server-side; issues short-lived JWTs to extension clients.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "seed": "tsx scripts/seed-users.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "@tsndr/cloudflare-worker-jwt": "^3.1.4"
  }
}
